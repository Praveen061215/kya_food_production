-- Check temperature_logs table structure
USE kya_food_production;

-- Show table structure
DESCRIBE temperature_logs;

-- Show all columns
SHOW COLUMNS FROM temperature_logs;

-- Count records
SELECT COUNT(*) as total FROM temperature_logs;
